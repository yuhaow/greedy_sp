To get the figures:
First run "data.R" to get the training data.
Second go to folder "sp" and run the script "./autosub.sh". 
Third run "additional_edge.R" to get the additional lines for greedy sp, and paste the additional lines into "lines.R".
Finally run "plot.R" to get the plots.
